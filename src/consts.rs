pub mod const_
{
    pub const COLUMNS: [&str; 3] = ["TODO", "IN PROGRESS", "DONE"];
    pub const MAX_COLUMNS: usize = COLUMNS.len();
    pub const JSON: &str = "data.json";

    pub const RS_ADDITEM: &str = "Add Item";
}
